# Codex MCP snippet for Termius CLI.
# Copy into ~/.codex/config.toml, then point `command` at your `termius` binary.

[mcp_servers.termius]
command = "termius"
args = ["mcp"]
startup_timeout_sec = 30.0
tool_timeout_sec = 60.0
