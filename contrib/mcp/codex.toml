# Codex MCP snippet for Termius MCP.
# Merge into ~/.codex/config.toml, then point `command` at your `termius` binary.

[mcp_servers.termius]
command = "termius"
args = []
startup_timeout_sec = 30.0
tool_timeout_sec = 60.0
